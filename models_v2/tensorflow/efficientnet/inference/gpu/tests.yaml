inference-efficientnetb0:
  cmd: bash $GITHUB_WORKSPACE/tests/cicd/ITEX-XPU/setup.sh ${FRAMEWORK_VERSION} ${FRAMEWORK_EXTENSION_VERSION} ${IS_LKG_DROP} ${AIKIT_VERSION}; bash $GITHUB_WORKSPACE/tests/cicd/ITEX-XPU/efficientnet-inference/run_test.sh ${MODEL_NAME} ${IS_LKG_DROP}
  env:
    MODEL_NAME: "EfficientNetB0"
inference-efficientnetb3:
  cmd: bash $GITHUB_WORKSPACE/tests/cicd/ITEX-XPU/setup.sh ${FRAMEWORK_VERSION} ${FRAMEWORK_EXTENSION_VERSION} ${IS_LKG_DROP} ${AIKIT_VERSION}; bash $GITHUB_WORKSPACE/tests/cicd/ITEX-XPU/efficientnet-inference/run_test.sh ${MODEL_NAME} ${IS_LKG_DROP}
  env:
    MODEL_NAME: "EfficientNetB3"
inference-efficientnetb4:
  cmd: bash $GITHUB_WORKSPACE/tests/cicd/ITEX-XPU/setup.sh ${FRAMEWORK_VERSION} ${FRAMEWORK_EXTENSION_VERSION} ${IS_LKG_DROP} ${AIKIT_VERSION}; bash $GITHUB_WORKSPACE/tests/cicd/ITEX-XPU/efficientnet-inference/run_test.sh ${MODEL_NAME} ${IS_LKG_DROP}
  env:
    MODEL_NAME: "EfficientNetB4"
